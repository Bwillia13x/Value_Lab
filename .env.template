# Environment variables for Pixel Wisdom – Value-Investor Performance Lab
# Copy this file to `.env.local` (for local dev) or set equivalent variables in your host (GitHub → Settings → Secrets, Vercel → Project Settings → Environment Variables).
# ---------------------------------------------------------------------------
# Yahoo Finance RapidAPI key
# Docs: https://rapidapi.com/apidojo/api/yahoo-finance1/  |  Free tier ≈ 500 req/day, 30 req/min. Paid tiers higher.
YF_KEY=

# St. Louis Fed – FRED API key
# Docs: https://fred.stlouisfed.org/docs/api/fred/  |  Limit: 120,000 requests / day, 1,000 req per hour.
FRED_KEY=

# Supabase project URL (e.g. https://xyzcompany.supabase.co)
SUPABASE_URL=

# Supabase public (anon) key – found under Project Settings → API → Project API keys.
# NOTE: This is safe to expose in client-side code but rotate if leaked.
SUPABASE_ANON=

# ---------------------------------------------------------------------------
# Usage notes
# – "npm run dev" and CI will fail early if any required var is missing.
# – For CI, GitHub Action "Check env template completeness" ensures every key in this file has a non-empty counterpart in the runtime env.
# – Keep this file alphabetically sorted to aid diff reviews.